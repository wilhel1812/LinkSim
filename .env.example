# No frontend auth variables required.
# Cloud auth is enforced by Cloudflare Access at the edge.
# VITE_MAPTILER_KEY remains required for deployment.
# VITE_CARTO_KEY is optional; when set, restrict the browser-visible key to the LinkSim domains.
