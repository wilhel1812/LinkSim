services:
  web:
    build:
      context: .
      target: production
    ports:
      - "8080:80"
    restart: unless-stopped

  dev:
    build:
      context: .
      target: development
    command: npm run dev -- --host 0.0.0.0 --port 5173
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
